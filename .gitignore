node_modules/
dist/
.DS_Store

# Secrets — only .env.example is committed
.env
.env.local
.env.*.local

# Private working notes, the verification harness, and the assignment's own materials.
# Ignored as a whole directory rather than file by file: the previous version listed each note
# by name, so every new one — a handoff, a backup, a scratch script — started out tracked.
.claude/
.impeccable/
docs/algolia-primer.md
# Product framing and the phase 3 surface spec. Both discuss the scorecard and the interview panel,
# which is working context rather than something a reviewer should be reading. DESIGN.md is
# committed, because a design system is a legitimate part of the deliverable.
PRODUCT.md
docs/search-ui-build-contract.md
